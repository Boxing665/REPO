# 複製此檔為 .env 填入實際值（本機開發用）
# Zeabur 上直接在服務的「環境變數」設定這些值

# MongoDB 連線字串（Zeabur MongoDB 服務提供）
MONGODB_URI=mongodb://localhost:27017/pangpang

# 管理員金鑰（自訂複雜密碼，用於 /api/admin/* 端點）
ADMIN_KEY=your_secret_admin_key_here

# 伺服器監聽 Port（Zeabur 自動設定，通常不需手動填）
PORT=3000
